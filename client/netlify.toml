# Netlify build configuration for the React (Vite) client.
[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback so deep links like /reset-password/:token resolve to index.html.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
